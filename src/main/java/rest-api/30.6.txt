EXPLAIN SELECT COUNT(*)
FROM READERS
WHERE FIRSTNAME = 'John';

CREATE INDEX FIRSTNAMENO ON READERS (FIRSTNAME);
CREATE INDEX LASTNAMENO ON READERS (LASTNAME);

EXPLAIN SELECT COUNT(*)
FROM READERS
WHERE FIRSTNAME = 'John';

CREATE INDEX TITLENO ON BOOKS (TITLE);